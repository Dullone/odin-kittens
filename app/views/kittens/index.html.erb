<p>
<%= link_to("Add a new kitten!", new_kitten_path) %>
</p>

<h1>Kittens:</h1>

<% @kittens.each do |kitten| %>

<div class="Kitten-show">
  <%= render partial: 'kitten', locals: { kitten: kitten } %>
  <%= link_to("edit", edit_kitten_path(kitten.id)) %>
  <%= link_to("delete", edit_kitten_path(kitten.id), mothod: :delete) %>
</div>
<br/>
<% end %>